# Tasks: a0-01-zen-mode-editable

- [ ] 更新 `openspec/changes/a0-01-zen-mode-editable/specs/editor/spec.md`，明确禅模式正文必须可编辑
- [ ] 让禅模式复用当前文档编辑状态，而不是只读内容提取
- [ ] 确保在禅模式中的输入会写回当前文档并进入既有保存链路
- [ ] 保持禅模式现有沉浸式 UI 约束与退出交互
- [ ] 为 enter / type / exit / empty document 场景补齐 renderer 级测试
